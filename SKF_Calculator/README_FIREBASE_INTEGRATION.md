# Firebase Integration - SKF Calculator

## ✅ Реализованная функциональность

### 🔐 Аутентификация пользователей
- Регистрация новых пользователей
- Вход в систему
- Выход из аккаунта
- Автоматическая проверка состояния аутентификации

### 📊 Три таблицы базы данных

#### 1. Пользователи (`users`)
- Базовая информация из Firebase Authentication
- Email, дата создания, последний вход

#### 2. Профили пользователей (`userProfiles`)
- Расширенная информация: ФИО, дата рождения, пол, раса, рост, вес
- Данные используются для автоматического заполнения форм расчетов

#### 3. Анализы (`analyses`)
- История всех медицинских расчетов
- Сохранение: возраст, пол, раса, рост, вес, ИМТ, креатинин, результаты СКФ
- Каждый пользователь видит только свои анализы

### 🔒 Безопасность данных
- Каждый пользователь видит только свои данные
- Все запросы фильтруются по `userId`
- Firebase Authentication обеспечивает защиту

## 🚀 Как использовать

### 1. Настройка Firebase Console
```bash
# Следуйте инструкциям в файле FIREBASE_SETUP.md
# Включите Authentication → Email/Password
```

### 2. Запуск приложения
```bash
npm install
npm start
```

### 3. Работа с приложением

#### Регистрация/Вход
- Заполните email и пароль
- Нажмите "Зарегистрироваться" или "Войти"
- Данные сохраняются в Firebase автоматически

#### Заполнение профиля
- Перейдите в раздел "Профиль"
- Заполните личные данные (ФИО, дата рождения, пол, раса, рост, вес)
- Эти данные будут автоматически использоваться для расчетов

#### Выполнение расчетов
- В разделе "Расчет" автоматически заполняются данные из профиля
- Введите уровень креатинина
- Нажмите "Рассчитать"
- Результат сохраняется в историю автоматически

#### Просмотр истории
- В разделе "История" отображаются все ваши анализы
- Только ваши данные (фильтр по userId)

## 📁 Структура файлов

```
SKF_Calculator/
├── types/
│   └── database.ts          # Типы данных для Firestore
├── services/
│   └── databaseService.ts   # Сервис для работы с базой данных
├── contexts/
│   └── AuthContext.tsx      # Контекст аутентификации + данные
├── pages/
│   ├── LoginScreen.tsx      # Экран входа
│   ├── RegisterScreen.tsx   # Экран регистрации
│   ├── HomeScreen.tsx       # Основной экран расчетов
│   ├── ProfileScreen.tsx    # Экран профиля пользователя
│   └── HistoryScreen.tsx    # История анализов
├── config/
│   └── firebase.ts          # Конфигурация Firebase
└── FIREBASE_SETUP.md        # Инструкции по настройке
```

## 🔧 Технические детали

### AuthContext расширения
```typescript
interface AuthContextType {
  user: User | null;
  loading: boolean;
  userProfile: UserProfile | null;      // Профиль пользователя
  userAnalyses: Analysis[];             // История анализов
  signIn: (email, password) => Promise<void>;
  signUp: (email, password) => Promise<void>;
  logout: () => Promise<void>;
  updateUserProfile: (updates) => Promise<void>;  // Обновление профиля
  saveAnalysis: (data) => Promise<string>;        // Сохранение анализа
  refreshUserData: () => Promise<void>;           // Перезагрузка данных
}
```

### Автоматическое заполнение данных
- При входе пользователя загружаются его профиль и история анализов
- Форма расчета автоматически заполняется данными из профиля
- Все изменения синхронизируются с Firebase в реальном времени

### Фильтрация данных
- Все запросы к Firestore фильтруются по `userId`
- Пользователь видит только свои данные
- Firebase Security Rules обеспечивают дополнительную защиту

## 🐛 Возможные проблемы

### "auth/configuration-not-found"
- **Решение**: Включите Authentication в Firebase Console
- Следуйте инструкциям в `FIREBASE_SETUP.md`

### Данные не отображаются
- **Решение**: Проверьте подключение к интернету
- Перезагрузите приложение
- Проверьте статус аутентификации

### Старые данные в AsyncStorage
- **Решение**: Новые данные сохраняются в Firebase
- Старые данные из AsyncStorage игнорируются

## 📈 Мониторинг и поддержка

### Просмотр данных в Firebase Console
1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `skfcalculator-7d6f8`
3. **Firestore Database** → Просмотр коллекций
4. **Authentication** → Просмотр пользователей

### Логи приложения
- Откройте Developer Tools в браузере
- Просмотрите Console для отладочной информации
- Все операции с Firebase логируются

## 🔄 Следующие шаги

1. **Настройка Firebase Security Rules** для дополнительной безопасности
2. **Добавление валидации данных** на стороне сервера
3. **Резервное копирование** критичных данных
4. **Добавление оффлайн режима** с синхронизацией
5. **Экспорт данных** в PDF/Excel для пользователей

---

**Приложение готово к использованию!** 🎉

Все пользователи могут регистрироваться, сохранять свои профили и историю анализов. Данные надежно хранятся в Firebase и доступны только авторизованным пользователям.